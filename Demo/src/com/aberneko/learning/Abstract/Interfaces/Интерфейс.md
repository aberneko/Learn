> #### `Интерфейс в Java` — это контракт, который определяет, что должен уметь объект, но не говорит, как именно это нужно делать
> __По сути интерфейс — это набор сигнатур методов, которые описывают функциональность объектов, относящихся к интерфейсу, но не реализуют её.__
- __Он содержит набор методов без реализации, которые должен реализовать любой класс, подписавшийся на этот контракт.__
- __Интерфейс позволяет описывать общее поведение для совершенно разных классов, не связывая их через наследование.__


> #### _💡 В Java названия интерфейсов как правило являются прилагательными и заканчиваются на суффикс `-able` (например, `Serializable`, `Clonable` или `Iterable`). Но иногда разработчики отступают от этого правила даже в коде стандартной библиотеки — например, когда интерфейс представляет собой семейство классов. В этом случае интерфейсы называют именами существительными (например, `List` и `Map`)._

**В интерфейсе можно объявить поля, но они всегда будут статическими константами. Поэтому в полях интерфейса сохраняют значения, которые нужно использовать в различных частях программы — их модификатор по умолчанию тоже будет `public`, как и у методов.**


> #### Но начиная с `Java 8`  в интерфейсах можно определять методы с реализацией — с помощью ключевого слова  `default.`

**Представьте, что вы добавили новый метод в интерфейс, который уже реализуют десятки классов. Без `default-метода` все эти классы перестанут компилироваться, потому что теперь обязаны реализовать новый метод.**
`default-методы` позволяют:

- _расширить интерфейс, не нарушая обратную совместимость;_
- _задать реализацию по умолчанию, которую класс может использовать или переопределить, если захочет._